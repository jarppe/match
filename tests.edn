#kaocha/v1
 #meta-merge
  [{:tests [{:id           :unit
             :test-paths   ["src/test"]
             :source-paths ["src/main"]
             :ns-patterns  ["match\\..*-test$"]}

            {:id           :unit-cljs
             :test-paths   ["src/test"]
             :source-paths ["src/main"]
             :ns-patterns  ["match\\..*-test$"]}

            {:id           :example
             :test-paths   ["src/test"]
             :source-paths ["src/main"]
             :ns-patterns  ["example\\..*-test$"]}

            {:id           :example-cljs
             :type         :kaocha.type/cljs
             :test-paths   ["src/test"]
             :source-paths ["src/main"]
             :ns-patterns  ["example\\..*-test$"]}]}]
